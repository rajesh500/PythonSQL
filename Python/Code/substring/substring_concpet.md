# substring patterns 
1. substring exists in string
   first occurance and all occurances.

while loop with find.

2. longest and shortest subarray without repeat characters in string 
   
3. Fixed-size sliding window (anagrams / exact length K)
   Use when substring length is fixed (e.g., len(p)).
   Find all anagrams of p in s

    sliding window with substring size till len(s)-len(ss)+1

4. Count substrings that satisfy a rule” (often “at most K …”)  ?

5. 